<p id="notice"><%= notice %></p>

<h1>Itineraries</h1>

<div id="search">
  <%= form_tag itineraries_path, method: :get do %>
    <%= text_field_tag( 'q', nil, class: "text", type: 'text', title:"Search", 
                       value: @q,  placeholder: "Code"  ) %> 
    <br/>
    <input type="submit" value="Find Itinerary Start ID", autofocus="true" /> 
  <%- end -%>
</div>


<table>
  <thead>
    <tr>
      <% prez.titles.each do |title| %>
        <th><%= title %></th>
      <% end  %>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% prez.each do |single_prez| %>
      <tr>            
        <% single_prez.values.each do | value | %>
          <td><%= exhibit(value, self) %> </td> 
        <% end %>
        <td><%= single_prez.link_to_show %></td>
        <td><%= single_prez.link_to_edit %></td>
        <td><%= single_prez.link_to_destroy %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= prez.link_to_new %>

